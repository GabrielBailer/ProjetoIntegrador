<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/fala_comigo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fala_comigo_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_PI2"
        tools:targetApi="31">

        <activity
            android:name=".activity.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="app-pi2.web.app"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.TelaLogin"
            android:exported="false" />

        <activity
            android:name=".ui.NovoCadastro"
            android:exported="false" />

        <activity
            android:name=".ui.RecuperarSenha"
            android:exported="false" />

        <activity
            android:name=".ui.NovaInteracao"
            android:exported="false" />

        <activity
            android:name=".ui.Home"
            android:exported="false" />

    </application>

</manifest>